# AGENTS.md

## 1. 项目定位

本项目是一个面向 2-4 人小团队的 Web 创作工作台，用于整合两个已有 Python 项目：

- AI_NovelGenerator（小说生成）
- novel_to_drama（小说转短剧脚本）

目标是提供从小说生成、章节编辑到短剧改编与导出的完整工作流。

这是一个中型 Web 应用，不是一次性原型，也不是企业级 SaaS 平台。

---

## 2. 单一真源原则

在开始任何开发任务前，必须优先阅读以下文件：

1. `docs/PRD.md`
2. `docs/ARCHITECTURE.md`
3. `docs/ROADMAP.md`
4. `docs/DATA_MODEL.md`
5. `docs/API_SPEC.md`
6. `memory-bank/progress.md`
7. `memory-bank/decisions.md`

如果这些文档与当前代码不一致，优先指出冲突，不要擅自假设。

---

## 3. 开发总原则

1. 优先保证系统结构清晰、可持续迭代，不要追求一次性写完全部功能。
2. 只实现当前任务明确要求的内容，不要擅自扩展功能范围。
3. 不允许为"看起来更完整"而主动引入额外框架、依赖或复杂设计。
4. 不允许在未说明影响范围的情况下重构大面积代码。
5. 不允许一次性修改大量无关文件。
6. 默认采用最小改动策略。
7. 如果改动属于大改，先输出 Plan，再开始改代码。

---

## 4. 技术栈约束

### 后端
- FastAPI
- SQLAlchemy 2.0
- Alembic
- PostgreSQL
- Redis
- 任务队列：RQ / Dramatiq / Celery（三选一，按当前项目实际为准）

### 前端
- React
- TypeScript
- Tailwind CSS
- Zustand
- React Query

### 禁止
- 不要擅自切换后端框架
- 不要擅自改成全栈单体框架
- 不要擅自引入与现有架构冲突的大型状态管理方案
- 不要擅自新增未被要求的云服务依赖

---

## 5. 代码组织规则

### 后端目录分层
- `core/`: 配置、安全、日志、常量
- `infra/`: 数据库、Redis、队列、向量存储等基础设施封装
- `models/`: SQLAlchemy 模型
- `schemas/`: Pydantic 请求/响应模型
- `routers/`: API 路由层，仅处理请求、参数校验、返回
- `services/`: 业务逻辑层
- `worker/` 或 `worker.py`: 异步任务入口

### 前端目录分层
- `api/`: API 请求封装
- `components/`: 通用 UI 组件
- `pages/`: 页面级组件
- `hooks/`: 自定义 hooks
- `store/`: 全局状态
- `types/`: 类型定义

### 规则
- 不要在 router 中写复杂业务逻辑
- 不要在 service 中直接写 HTTP 层逻辑
- 不要让前端页面直接拼接复杂业务规则
- 公共逻辑优先抽离，不要复制粘贴

---

## 6. 旧项目复用规则

允许复用的是"核心业务逻辑"，不是旧项目的"运行形态"。

### 可以复用
- prompt 模板
- LLM adapter 工厂逻辑
- embedding adapter 逻辑
- 小说生成核心算法逻辑
- 短剧改编核心映射逻辑
- 导出逻辑

### 必须改造
- 文件读写 → 数据库读写
- CLI 参数输入 → API 输入
- 本地路径依赖 → 项目级资源管理
- GUI / 命令行入口 → Web service 调用

### 禁止
- 不要直接复制 CLI 主函数作为后端入口
- 不要保留本地 JSON 文件作为主业务存储
- 不要把桌面 GUI 状态逻辑带入 Web 项目

---

## 7. 数据模型规则

1. MVP 阶段优先使用简化模型，避免过度拆表。
2. 半结构化项目内容优先放入 `project_assets`。
3. 数据库变更前必须说明：
   - 变更原因
   - 影响范围
   - 是否影响已有迁移
4. 所有模型变更后，需要同步更新 `docs/DATA_MODEL.md`。

---

## 8. API 规则

1. API 路径统一使用 kebab-case 或清晰 REST 风格，不要混乱命名。
2. 所有新增接口必须同步更新 `docs/API_SPEC.md`。
3. 长耗时 AI 任务不能在 HTTP 请求中直接同步执行。
4. 生成类操作必须走任务队列。
5. 返回结构保持一致，错误响应统一。

---

## 9. 异步任务规则

1. 小说生成、短剧改编、导出等长耗时操作必须异步化。
2. API 层只负责：
   - 参数校验
   - 创建任务
   - 返回 task_id
3. 任务执行过程必须写入状态与日志。
4. 支持后续取消与重试，不要把任务写成不可恢复黑盒。

---

## 10. 文档与变更记录规则

每次完成一个中等以上改动，必须做以下事情：

1. 更新 `memory-bank/progress.md`
2. 如涉及架构决策，更新 `memory-bank/decisions.md`
3. 如涉及接口变更，更新 `docs/API_SPEC.md`
4. 如涉及数据模型变更，更新 `docs/DATA_MODEL.md`
5. 在 `docs/CHANGELOG.md` 记录本次修改内容

---

## 11. Git 与改动控制规则

1. 每次开发前先检查当前分支状态。
2. 大改动必须在新分支进行。
3. 小步提交，不要累计大量未提交修改。
4. 改完后必须先看 diff，再决定是否继续。
5. 不允许在没有确认的情况下批量覆盖文件。

推荐流程：

- `git status`
- `git diff`
- 确认无误后再提交

---

## 12. 大改流程

如果任务属于以下情况之一，视为大改：

- 新增完整模块
- 调整数据库结构
- 重构核心流程
- 引入新基础设施
- 改动多个层级（数据层 + API 层 + UI 层）

大改必须遵循以下流程：

1. 先存档 / 新建分支
2. 先写本次迭代 PRD
3. 先输出实施计划（Plan）
4. 按步骤逐步实现
5. 更新 Changelog 与 Memory Bank

---

## 13. 输出要求

在每次开始改代码前，你应先输出：

1. 对当前任务的理解
2. 影响范围
3. 将修改哪些文件
4. 是否属于大改
5. 如果是大改，先给 Plan，不直接动代码

在每次完成改动后，你应输出：

1. 修改了哪些文件
2. 每个文件改了什么
3. 是否需要迁移 / 测试 / 运行命令
4. 是否需要同步更新文档

---

## 14. 禁止事项

1. 不要擅自重命名大批量目录或文件
2. 不要为临时修 bug 破坏现有分层
3. 不要通过复制代码快速堆功能
4. 不要引入"看起来高级"但当前不需要的模式
5. 不要跳过文档更新
6. 不要一次性做多个无关问题
7. 不要在未审查 diff 的情况下大规模写入

---

## 15. 协作目标

你的角色不是"自由发挥的全自动写码器"，而是：

- 需求理解助手
- 实施计划助手
- 受约束的代码实现者
- 变更可追溯的协作伙伴

默认目标：

- 小步推进
- 可回滚
- 可解释
- 可维护
- 可持续迭代
