# CLAUDE.md — 项目工作原则

> 本文件是 Claude Code 在本项目中的工作准则，与 `AGENTS.md` 的协作规范一起生效。

## 核心原则

### 1. 不通过删除代码来解决问题

排查和修复问题时要**先找到根因**，不要通过删掉代码来绕过问题。在删除任何功能、接口或代码片段前，必须先说明删除理由和影响范围，并征得确认。

### 2. 新增功能不得阉割其他功能

任何新增功能或改动，都不能破坏、削弱或隐藏现有功能。改动涉及前端/后端/生成流程时，必须回归验证相关既有功能（登录、项目管理、章节、架构/目录/章节生成、短剧改编、导出、AI 问答等）。

### 3. 参数/接口/配置调整必须同步更新文档

每次调整配置项、环境变量、接口、数据模型、prompt 或默认参数，都要**及时**同步更新对应文档：

- `docs/API_SPEC.md` —— 接口变更
- `docs/CHANGELOG.md` —— 变更记录
- `docs/DATA_MODEL.md` —— 数据模型变更
- `docs/ARCHITECTURE.md` —— 架构变更
- `memory-bank/progress.md` / `memory-bank/decisions.md` —— 进度与决策
- `.env.example` / `.env.production.example` 等配置模板

### 4. 冗余代码及时删除

被替代、已无用、无引用的代码及时清理，保持仓库整洁。删除前用搜索确认无引用。

## 执行要求

- 每次改动前：说明理解、影响范围、将修改哪些文件
- 每次改动后：回归验证既有功能，更新文档，报告修改清单
- 详细的协作流程见 `AGENTS.md`
