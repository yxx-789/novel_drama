# CLAUDE.md — 项目工作原则

> 本文件是 Claude Code 在本项目中的工作准则，与 `AGENTS.md` 的协作规范一起生效。

## 核心原则

### 1. 不通过删除代码来解决问题

排查和修复问题时要**先找到根因**，不要通过删掉代码来绕过问题。在删除任何功能、接口或代码片段前，必须先说明删除理由和影响范围，并征得确认。

### 2. 新增功能不得阉割其他功能

任何新增功能或改动，都不能破坏、削弱或隐藏现有功能。改动涉及前端/后端/生成流程时，必须回归验证相关既有功能（登录、项目管理、章节、架构/目录/章节生成、短剧改编、导出、AI 问答等）。

### 3. 参数/接口/配置调整必须同步更新文档

每次调整配置项、环境变量、接口、数据模型、prompt 或默认参数，都要**及时**同步更新对应文档：

- `docs/API_SPEC.md` —— 接口变更
- `docs/CHANGELOG.md` —— 变更记录
- `docs/DATA_MODEL.md` —— 数据模型变更
- `docs/ARCHITECTURE.md` —— 架构变更
- `memory-bank/progress.md` / `memory-bank/decisions.md` —— 进度与决策
- `.env.example` / `.env.production.example` 等配置模板

### 4. 冗余代码及时删除

被替代、已无用、无引用的代码及时清理，保持仓库整洁。删除前用搜索确认无引用。

## 执行要求

- 每次改动前：说明理解、影响范围、将修改哪些文件
- 每次改动后：回归验证既有功能，更新文档，报告修改清单
- 详细的协作流程见 `AGENTS.md`

---

## V3 生成系统开发专项原则（主 agent + 所有子 agent）

本专项覆盖「积木式生成系统」的全部开发：积木库（用户层 7 维 + 内部细粒度风味库）、创作意图注入、减法 prompt 改造、受众适配、记忆优化。**主 agent 和所有派发的子 agent 都必须遵守本原则**，且主 agent 派发子 agent 时，必须把本文件的相关要求明确写入子 agent 的任务描述，不能指望子 agent 自行读取。

### 积木/模块质量底线（不可妥协）

1. **每个积木、每个模块都必须认认真真完成**——宁缺毋滥，绝不为了"完成任务"而降低任何一个积木的质量。
2. **质量 > 速度**：任务完成度不以牺牲积木质量为代价。若某个积木内容量大，允许分批、分阶段交付，但**不允许做糙、做简、做空**。
3. **严禁"能跑就行"**：每个积木必须有完整、准确、有深度的语义内容（文风特征、卖点套路、避雷、典型场景、示例片段等），不是占位符、不是敷衍一句话。
4. **正交不重叠**：各维度积木按 spec 定义互不重叠、职责单一；发现重叠要修正，而非含糊合并或用泛化话术掩盖。

### 子 agent 执行要求

5. 子 agent 实现某个积木/模块时，以 spec 的定义为准，**不得自行简化、删减或稀释要求**。
6. 子 agent 若遇到"积木内容过多、单次做不完"的情况，必须如实报告并请求拆分，**不得悄悄降质完成**。
7. 子 agent 的完成标准 = spec 定义的积木完整实现 + 验证通过，**不是"代码跑通了"**。
8. 子 agent 不得为了省 token / 省时间而跳过积木的某部分（如只写一半风格特征、省略避雷等）。

### 验收标准

9. 每个积木**独立可验证**：内容完整性、与其他积木的正交性、注入 prompt 后的实际效果。
10. 主 agent 评审子 agent 产出时，重点检查：**是否为了赶进度而降低了某个积木的质量**。发现降质必须打回，不允许放行。
